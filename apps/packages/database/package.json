{
  "name": "@repo/database",
  "version": "0.0.0",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",

    "generate": "prisma generate",

    "prisma:migrate:local": "env-cmd -f ../../../.env.local prisma migrate dev",

    "prisma:seed:local": "env-cmd -f ../../../.env.local prisma db seed",

    "prisma:migrate:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.3.0",
    "@prisma/client": "^7.3.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "dotenv": "^17.2.4",
    "env-cmd": "^11.0.0",
    "prisma": "^7.4.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
